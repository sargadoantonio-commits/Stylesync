import "package:flutter/material.dart";

/// Placeholder for PaymentOverlay - to be implemented when service providers are available
/// This file is currently not used in the active app flow
class PaymentOverlay extends StatelessWidget {
  const PaymentOverlay({super.key, required this.service});
  final dynamic service;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
